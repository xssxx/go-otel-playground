# OpenTelemetry OTLP gRPC endpoint (host:port, no scheme)
# Use "localhost:4317" when running the app directly
# Use "jaeger:4317" when running via docker-compose
OTEL_EXPORTER_OTLP_ENDPOINT=localhost:4317
